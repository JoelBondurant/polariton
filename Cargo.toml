[package]
name = "polariton"
version = "0.1.0"
edition = "2024"
resolver = "3"
authors = ["Joel Bondurant"]
description = "A strong coupling of data and visualization."


[dependencies]
async-trait = "0.1.89"
iced = { version = "0.14.0", features = ["advanced"] }
polars = { version = "0.53.0", features = ["lazy", "sql"] }
tokio = { version = "1.49.0", features = ["full"] }
tokio-rusqlite = { version = "0.7.0", features = ["bundled"] }


[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
debug = 0
